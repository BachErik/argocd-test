apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx-download
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30000
    protocol: TCP
    targetPort: 2244
  selector:
    app: nginx
